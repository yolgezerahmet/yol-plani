{
  "name": "yol-plani",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IONIQ 5 için bölüm bazlı hız, tüketim, süre ve şarj planlayıcı",
  "scripts": {
    "test": "node --test",
    "android": "cap add android && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
